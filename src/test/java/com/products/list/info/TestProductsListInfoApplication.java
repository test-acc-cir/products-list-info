package com.products.list.info;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.test.context.TestConfiguration;
import org.springframework.context.annotation.Bean;

@TestConfiguration(proxyBeanMethods = false)
public class TestProductsListInfoApplication {

	/*
	 * @Bean
	 * 
	 * @ServiceConnection MongoDBContainer mongoDbContainer() { return new
	 * MongoDBContainer("mongo:latest"); }
	 * 
	 * public static void main(String[] args) {
	 * SpringApplication.from(ProductsListInfoApplication::main).with(
	 * TestProductsListInfoApplication.class).run(args); }
	 */

}
